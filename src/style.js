import Styled from 'styled-components';

const HireStyle = Styled.div`
    > div {
        min-height: 80vh;
        background-size: cover;
        padding: 50px 0;
    }
`;

export {
    HireStyle
};